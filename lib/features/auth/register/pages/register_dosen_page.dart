import 'package:flutter/material.dart';
import 'package:pass_pens/features/auth/register/widgets/form_dosen.dart';

class RegisterDosenPage extends StatelessWidget {
  const RegisterDosenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(backgroundColor: Colors.white, body: FormDosen());
  }
}
